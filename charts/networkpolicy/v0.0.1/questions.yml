questions:
- variable: access.type
  default: "not-allow"
  description: "allow access between a & b"
  type: enum
  options:
    - "not-allow"
    - "allow"
  required: true
  label: allow access between a & b
